apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-gateway-config
  namespace: monitor
data:
  gateway.hcl: |
    ############################
    # OTLP RECEIVER
    ############################
    otelcol.receiver.otlp "otlp" {
      grpc {}
      http {}

      output {
        traces  = [otelcol.processor.batch.default.input]
        metrics = [otelcol.processor.batch.default.input]
        logs    = [otelcol.processor.batch.logs.input]
      }
    }

    ############################
    # BATCH PROCESSOR (TRACE + METRIC)
    ############################
    otelcol.processor.batch "default" {
      output {
        traces  = [otelcol.processor.tail_sampling.apm.input]
        metrics = [otelcol.exporter.prometheus.default.input]
      }
    }

    ############################
    # BATCH PROCESSOR (LOGS)
    ############################
    otelcol.processor.batch "logs" {
      output {
        logs = [otelcol.exporter.loki.apm.input]
      }
    }

    ############################
    # TAIL SAMPLING (APM)
    ############################
    otelcol.processor.tail_sampling "apm" {
      policy {
        name = "errors"
        type = "status_code"
        status_code {
          status_codes = ["ERROR"]
        }
      }

      policy {
        name = "slow"
        type = "latency"
        latency {
          threshold_ms = 2000
        }
      }

      policy {
        name = "baseline"
        type = "probabilistic"
        probabilistic {
          sampling_percentage = 10
        }
      }

      output {
        traces = [otelcol.exporter.otlp.tempo.input]
      }
    }

    ############################
    # TEMPO EXPORTER (TRACES)
    ############################
    otelcol.exporter.otlp "tempo" {
      client {
        endpoint = "tempo.monitor.svc:4317"
        tls {
          insecure = true
        }
      }
    }

    ############################
    # PROMETHEUS EXPORTER (METRICS)
    ############################
    otelcol.exporter.prometheus "default" {
      forward_to = [prometheus.remote_write.mimir.receiver]
    }

    prometheus.remote_write "mimir" {
      endpoint {
        url = "http://mimir-distributor.monitor.svc:8080/api/v1/push"
      }
    }

    ############################
    # LOKI EXPORTER (LOGS)
    ############################
    otelcol.exporter.loki "apm" {
      forward_to = [loki.write.apm.receiver]
    }

    loki.write "apm" {
      endpoint {
        url = "http://loki.monitor.svc:3100/loki/api/v1/push"
      }
    }
