apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-agent-config
  namespace: monitor
data:
  agent.hcl: |
    // =========================
    // K8S LOG COLLECTION
    // =========================
    discovery.kubernetes "pods" {
      role = "pod"
    }

    loki.source.kubernetes "pods" {
      targets    = discovery.kubernetes.pods.targets
      forward_to = [loki.write.default.receiver]
    }

    loki.write "default" {
      endpoint {
        url = "http://loki.monitor.svc:3100/loki/api/v1/push"
      }
    }

    // =========================
    // OTLP RECEIVER
    // =========================
    otelcol.receiver.otlp "default" {
      grpc {}
      http {}

      output {
        traces  = [otelcol.processor.attributes.env.input]
        metrics = [otelcol.processor.attributes.env.input]
      }
    }

    // =========================
    // ADD STATIC ATTRIBUTE (ALLOY CORRECT)
    // =========================
    otelcol.processor.attributes "env" {
      attribute {
        key   = "deployment.environment"
        value = "demo"
      }

      output {
        traces  = [otelcol.exporter.otlp.gateway.input]
        metrics = [otelcol.exporter.otlp.gateway.input]
      }
    }

    // =========================
    // EXPORT TO GATEWAY
    // =========================
    otelcol.exporter.otlp "gateway" {
      client {
        endpoint = "alloy-gateway.monitor.svc:4317"
        tls {
          insecure = true
        }
      }
    }
